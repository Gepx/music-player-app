import 'package:flutter_test/flutter_test.dart';

void main() {
  test('widget test placeholder', () {
    // This project includes platform-specific (web-only) playback code which is
    // not available in the default VM test runner. Keep widget tests here
    // minimal unless using conditional imports or integration tests.
    expect(true, isTrue);
  });
}
